<h1>Widget Index</h1>
<% if owner? %>
  <ul>
    <li><%= link_to 'Add widget', new_widget_path %></li>
    <li><%= link_to 'Invite user', new_invitation_path %></li>
  </ul>
<% end %>
<% @widgets.each do |widget| %>
  <%= link_to widget.name, widget_path(widget) %>
<% end %>

